@extends('layouts.admin')
@section('title', 'Zamówienia | CopyCabana')
@section('content')
<div class="flex flex-wrap items-end justify-between gap-4"><div><p class="text-sm text-slate-500">Obsługa sprzedaży</p><h1 class="text-3xl font-bold">Zamówienia</h1></div><a class="rounded bg-white px-3 py-2 text-sm shadow" href="{{ route('admin.exports.orders') }}">Eksport CSV</a></div>
<form class="mt-6 flex flex-wrap gap-3 rounded-xl bg-white p-4 shadow-sm"><input name="q" value="{{ request('q') }}" placeholder="Numer, klient lub e-mail" class="rounded border p-2"><select name="status" class="rounded border p-2"><option value="">Wszystkie statusy</option>@foreach($statuses as $key => $label)<option value="{{ $key }}" @selected(request('status') === $key)>{{ $label }}</option>@endforeach</select><button class="rounded bg-[#063A60] px-4 py-2 text-white">Filtruj</button></form>
<div class="mt-5 overflow-x-auto rounded-xl bg-white shadow-sm"><table class="w-full min-w-[700px] text-left text-sm"><thead><tr class="border-b text-slate-500"><th class="p-4">Numer</th><th class="p-4">Klient</th><th class="p-4">Data</th><th class="p-4">Status</th><th class="p-4">Płatność</th><th class="p-4 text-right">Suma</th></tr></thead><tbody>@forelse($orders as $order)<tr class="border-b"><td class="p-4"><a class="font-semibold text-[#D51A70]" href="{{ route('admin.orders.show', $order) }}">{{ $order->number }}</a></td><td class="p-4">{{ $order->customer_name }}<br><span class="text-xs text-slate-500">{{ $order->customer_email }}</span></td><td class="p-4">{{ $order->created_at->format('d.m.Y H:i') }}</td><td class="p-4">{{ $order->status->label() }}</td><td class="p-4">{{ $order->payment_status }}</td><td class="p-4 text-right">{{ number_format((float) $order->total, 2, ',', ' ') }} zł</td></tr>@empty<tr><td colspan="6" class="p-8 text-center text-slate-500">Brak zamówień.</td></tr>@endforelse</tbody></table></div><div class="mt-4">{{ $orders->links() }}</div>
@endsection
