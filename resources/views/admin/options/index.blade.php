@extends('layouts.admin')
@section('title', 'Opcje i ceny | CopyCabana')
@section('content')
<div class="flex flex-wrap items-end justify-between gap-4"><div><p class="text-sm text-slate-500">Konfiguratory produktów</p><h1 class="text-3xl font-bold">Opcje i ceny</h1></div><a class="rounded bg-[#D51A70] px-4 py-2 font-semibold text-white" href="{{ route('admin.options.create') }}">Dodaj opcję</a></div>
<div class="mt-6 grid gap-4 md:grid-cols-2 xl:grid-cols-3">@forelse($options as $option)<article class="rounded-xl bg-white p-5 shadow-sm"><div class="flex justify-between gap-3"><h2 class="font-semibold">{{ $option->name }}</h2><span class="rounded bg-slate-100 px-2 py-1 text-xs">{{ $option->is_active ? 'Aktywna' : 'Nieaktywna' }}</span></div><p class="mt-2 text-sm text-slate-500">{{ $option->code }} · {{ $option->input_type === 'multi' ? 'wiele wartości' : 'jedna wartość' }} · {{ $option->pricing_model === 'percentage' ? '%' : 'zł' }}</p><p class="mt-3 text-sm">{{ $option->values_count }} wartości · {{ $option->products->count() }} produktów</p><div class="mt-4 flex justify-between"><a class="text-sm font-semibold text-[#D51A70]" href="{{ route('admin.options.edit', $option) }}">Edytuj</a><form method="post" action="{{ route('admin.options.destroy', $option) }}">@csrf @method('delete')<button class="text-sm text-red-600">Usuń</button></form></div></article>@empty<div class="rounded-xl bg-white p-8 text-slate-500 shadow-sm">Nie dodano jeszcze opcji.</div>@endforelse</div><div class="mt-4">{{ $options->links() }}</div>
@endsection
