@extends('layouts.admin')
@section('title', 'Klienci | CopyCabana')
@section('content')
<div class="flex flex-wrap items-end justify-between gap-4"><div><p class="text-sm text-slate-500">Dane klientów i prywatność</p><h1 class="text-3xl font-bold">Klienci</h1></div><a class="rounded bg-white px-3 py-2 text-sm shadow" href="{{ route('admin.exports.clients') }}">Eksport CSV</a></div>
<form class="mt-6 flex gap-3 rounded-xl bg-white p-4 shadow-sm"><input name="q" value="{{ request('q') }}" placeholder="Nazwa, firma lub e-mail" class="flex-1 rounded border p-2"><button class="rounded bg-[#063A60] px-4 py-2 text-white">Szukaj</button></form>
<div class="mt-5 overflow-x-auto rounded-xl bg-white shadow-sm"><table class="w-full min-w-[700px] text-left text-sm"><thead><tr class="border-b text-slate-500"><th class="p-4">Klient</th><th class="p-4">Firma</th><th class="p-4">Zamówienia</th><th class="p-4">Marketing</th><th class="p-4">RODO</th></tr></thead><tbody>@foreach($clients as $client)<tr class="border-b"><td class="p-4"><a class="font-semibold text-[#D51A70]" href="{{ route('admin.clients.show', $client) }}">{{ $client->name }}</a><br><span class="text-xs text-slate-500">{{ $client->email }}</span></td><td class="p-4">{{ $client->company ?: '—' }}</td><td class="p-4">{{ $client->orders_count }}</td><td class="p-4">{{ $client->marketing_consent ? 'Tak' : 'Nie' }}</td><td class="p-4">{{ $client->anonymized_at ? 'Zanonimizowany' : ($client->deletion_requested_at ? 'Wniosek' : 'Aktywny') }}</td></tr>@endforeach</tbody></table></div><div class="mt-4">{{ $clients->links() }}</div>
@endsection
